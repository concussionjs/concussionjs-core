$.ajaxSetup({
	cache: true
});

window.onload = function(){
			cjs.getPage(function(page){
				var error;
				//console.log($.stringify(cjs.inferObjects(page.html)));
				//console.log("countme: " + (countme++));
				$.post("http://<%=CJS_WEB_URL%>/postGetScript/true/?sid=<%=tenantId%>",JSON.stringify(cjs.inferObjects(page.html)),function(result){
						//console.log(result);
						eval(result);							
					});
			});
}

